spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:gp_test_db
  jpa:
    hibernate:
      ddl-auto: create-drop

base:
    url: test

s3:
  access-key: fakeKey
  region: fra1
  secret-key: fakeSecretKey
  endpoint: https://fake.com


